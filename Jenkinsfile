library 'ambar-pipeline-lib'

pipeline {
   agent any

   stages {
      stage('Ambar Original Stages ') {
         steps {
            martaPipeline()
         }
      }
      stage('Checking QA Replay') {
         steps {
            echo 'Bye World'
         }
      }
   }
}
